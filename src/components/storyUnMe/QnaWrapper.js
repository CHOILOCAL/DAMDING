import React from 'react';
import '../../css/QnaWrapper.css';

const QnaWrapper = ({children}) => {
    return (
        <div className="QnaWrapper">
            {children}
        </div>
    )
}

export default QnaWrapper;